name: SCimage 自动打标签

on:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: scimage-auto-tag-${{ github.ref }}
  cancel-in-progress: false

jobs:
  tag:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: SCimage

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: 打标签前运行测试
        shell: bash
        run: python -m unittest discover -s tests

      - name: 读取版本号
        id: version
        shell: bash
        run: |
          APP_VERSION="$(tr -d '[:space:]' < VERSION)"
          TAG_BASE="v${APP_VERSION}-build.${GITHUB_RUN_NUMBER}"
          TAG_NAME="${TAG_BASE}"
          TAG_SUFFIX=0
          while git ls-remote --exit-code --tags origin "refs/tags/${TAG_NAME}" >/dev/null 2>&1; do
            TAG_SUFFIX=$((TAG_SUFFIX + 1))
            TAG_NAME="${TAG_BASE}.${TAG_SUFFIX}"
          done
          echo "app_version=${APP_VERSION}" >> "${GITHUB_OUTPUT}"
          echo "tag_name=${TAG_NAME}" >> "${GITHUB_OUTPUT}"

      - name: 创建并推送标签
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ steps.version.outputs.tag_name }}" -m "SCimage release ${{ steps.version.outputs.tag_name }}"
          git push origin "${{ steps.version.outputs.tag_name }}"
